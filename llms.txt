# Axocoatl — the local-first workbench for coding agents
> Open source, local first, and centered on one durable Session

Axocoatl is an Apache-2.0 coding-agent workbench. Open a repository and give one
configured Agent—or a configured team—a durable, folder-anchored Session. The
browser app keeps conversation at the center while Files, Source Control,
Preview, Terminal, History, Agent activity, and Settings open around it.

Each accepted Turn records its request and context before execution, then keeps
the Agent output, final lifecycle state, and bounded tool Route in durable
History. Source Control separately shows the repository as it exists now;
Axocoatl does not make a commit or push unless the user deliberately asks for
that Git action.

## Start

```bash
# Install a checksum-verified native release on supported macOS or glibc Linux.
curl -fsSL https://axocoatl.ai/install.sh | sh

# Configure a provider and starter project, check host prerequisites, and run.
axocoatl onboard
axocoatl doctor
axocoatl dev
```

Open http://localhost:8080. The default listener is loopback-only.

Install from Cargo instead with `cargo install axocoatl-cli` and Rust 1.88 or
newer. Native Windows is not a release target; Windows users run Axocoatl through
WSL2.

## Product model

- **Workspace**: a durable, named identity for one authorized project directory.
- **Session**: one durable work item and conversation inside a Workspace.
- **Turn**: one accepted request, immutable context references, output, bounded
  tool evidence, and final lifecycle state in canonical Session History.
- **Attempt**: one candidate solution. When uncertainty warrants it, a Session
  can explore several isolated Ways and compare their Outcome, Route, available
  diff, Checks, known usage and cost, and optional Judge evidence.
- **Keep this one**: return one passing, non-empty Way to the primary Session
  checkout as an ordinary uncommitted Git change for review.

The ordinary path is one Agent or configured team in one Session. Exploring
several Ways is optional decision support, not the default interaction model.
The 1.0 Ways boundary requires an autonomous single-Agent Session and local Podman; Session
attachments, MCP tools, Skills, and web search are withheld from attempts.

## Runtime underneath

A Rust daemon supervises configured Agent actors and owns the canonical Session
ledger, checkpoint recovery, memory, provider adapters, tool execution, typed
events, Automations, and sandbox lifecycle.

- Providers: Ollama, OpenAI, OpenRouter, Anthropic, Gemini, Mistral, and
  OpenAI-compatible endpoints.
- Tools: bounded built-ins plus approved tools discovered from configured stdio
  or streamable-HTTP MCP servers. Axocoatl can also expose Agents through its MCP
  server command.
- Memory: live Session context, append-only daily logs, editable core-memory
  blocks, and on-device semantic recall. Checkpoints are execution-recovery
  caches; canonical History is the product record.
- Automations: explicit durable graphs configured and reviewed in Settings,
  with manual, interval, event, or Skill triggers and separate run history.
- Coordination: a configured coordinator Agent can decompose a goal, assign
  work to configured workers, run them in parallel, and synthesize the result.
  The typed event lattice carries notifications; it is not the Session, Agent
  graph, Ways model, or Automation graph.

## Isolation and network boundary

Rootless Podman is the local sandbox default. E2B Cloud is an explicit remote
option for normal Session work. A local Workspace remains a read-write host bind
inside its sandbox, so the boundary limits repository tools; it is not a defense
against an authorized tool making an unwanted change within that Workspace.

Axocoatl adds no product telemetry, hosted control plane, or Axocoatl account.
Local-first does not mean every configured path is offline. Hosted model
providers, MCP servers, web search, webhooks, the optional E2B Cloud sandbox,
Podman image and package downloads, repository traffic, and the one-time Hugging
Face embedding-model download can use the network. `sandbox.network: none`
blocks only code and commands inside the local Podman sandbox; daemon-side
integrations remain separate paths.

## Selected interfaces

```text
axocoatl onboard                 Interactive setup
axocoatl doctor                  Environment and dependency checks
axocoatl validate <config>       Validate configuration
axocoatl dev | serve             Run the daemon and browser product
axocoatl chat -a <agent>         Compatibility terminal chat
axocoatl workflow list | run     Compatibility view/run for Automations
axocoatl agents list|status|restart
axocoatl tokens report
axocoatl mcp servers|tools
```

The browser product has one interactive route at `/`. The HTTP and WebSocket APIs
remain available for integrations; the authoritative route reference is in the
documentation.

## Authoritative references

- Product model: https://github.com/axocoatl/axocoatl/blob/main/docs/PRODUCT.md
- Architecture: https://github.com/axocoatl/axocoatl/blob/main/docs/ARCHITECTURE.md
- Documentation: https://docs.axocoatl.ai/
- Install guide: https://docs.axocoatl.ai/start/install/
- Security boundary: https://docs.axocoatl.ai/operate/security/
- Source and changelog: https://github.com/axocoatl/axocoatl
