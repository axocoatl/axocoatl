---
title: Axocoatl documentation
description: Install Axocoatl, run the workbench, configure the runtime, and operate its local data safely.
template: splash
hero:
  tagline: One durable Session. The whole workspace around it.
  actions:
    - text: Install Axocoatl
      link: /start/install/
      icon: right-arrow
      variant: primary
    - text: Use the workbench
      link: /workbench/session/
      icon: right-arrow
---

import { Card, CardGrid, Steps } from '@astrojs/starlight/components';

Axocoatl is a local-first coding workbench backed by a durable Rust agent
runtime. A folder-anchored Session keeps conversation, context, files, terminal,
Preview, tools, history, and Git review together. Start with the Agent
or configured team that fits the work. Explore several Ways only when the task
warrants comparison.

## The shortest path

<Steps>

1. [Install the binary](/start/install/).
2. Run [`axocoatl onboard`](/start/onboard/) to configure Axocoatl for this user, then run [`axocoatl doctor`](/start/onboard/#run-doctor). Onboarding does not create a project or Workspace.
3. Start `axocoatl dev`, open `http://localhost:8080`, choose **Open workspace…**, and [create a Session](/start/first-session/).
4. Ask for a bounded change, review what happened, run the repository checks, and inspect Git.

</Steps>

## Pick the task in front of you

<CardGrid>
  <Card title="Start" icon="rocket">
    Install, create the user-level configuration, run doctor, and open your first Workspace and Session.
    [Begin →](/start/install/)
  </Card>
  <Card title="Use the workbench" icon="puzzle">
    Work in Conversation, open Files, Terminal, Preview, History, and Git, and use several Ways when one answer is not enough.
    [Open the guide →](/workbench/session/)
  </Card>
  <Card title="Configure" icon="setting">
    Set providers, Agents, sandboxes, Skills, MCP servers, and Automations.
    [Configure Axocoatl →](/configure/settings/)
  </Card>
  <Card title="Operate" icon="information">
    Run the service, back up local state, upgrade deliberately, and diagnose failures.
    [Operations guide →](/operate/service/)
  </Card>
  <Card title="Understand" icon="open-book">
    Learn the ownership boundaries behind Sessions, attempts, memory, events, and protocols.
    [Read the model →](/understand/product-model/)
  </Card>
  <Card title="Reference" icon="document">
    Look up CLI commands, configuration fields, HTTP routes, WebSocket frames, and examples.
    [Open reference →](/reference/cli/)
  </Card>
</CardGrid>

## What stays local—and what may not

The daemon and default Podman sandbox run on your machine, and Axocoatl adds no
product telemetry. That does not mean every configured path is offline. Hosted
model providers, remote MCP servers, web search, webhooks, bridged sandbox
networking, embedding-model downloads, and an E2B Cloud remote sandbox can
send data over the network. [Review the security boundary before using untrusted
input →](/operate/security/)
