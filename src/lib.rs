//! Axocoatl workspace root — exists only to host Criterion benchmarks in `benches/`.
//! All real code lives in the workspace member crates under `crates/`.

/// Contract validation shared by the resource-footprint benchmark and its tests.
pub mod resource_footprint_contract;
